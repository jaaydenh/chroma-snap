{
  "name": "@chroma-snap/action",
  "version": "0.1.0",
  "description": "GitHub Action wrapper around the Chroma Snap CLI.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "files": ["dist", "action.yml"]
}
