{
  "name": "@chroma-snap/shared",
  "version": "0.1.0",
  "description": "Shared config, manifest, upload, and review protocol types for Chroma Snap.",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "files": ["dist"]
}
