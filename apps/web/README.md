# @chroma-snap/web

Hosted review UI skeleton. The current implementation renders comparison reports as static HTML and includes a small local server. A full React review UI with signed URLs, permissions, approvals, audit events, zoom/pan, and keyboard navigation is a later milestone.
